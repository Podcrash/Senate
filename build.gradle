plugins {
    id 'com.github.johnrengelman.shadow' version '5.2.0'
    id 'java'
}

group 'com.podcrash'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'redis.clients', name: 'jedis', version: '3.2.0'
    implementation group: 'com.sparkjava', name: 'spark-core', version: '2.8.0'
    implementation group: 'org.mongodb', name: 'mongodb-driver-reactivestreams', version: '1.13.1'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
